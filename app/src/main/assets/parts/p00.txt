<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
<title>双色球缩水 V3.4</title>
<style>
:root{--bg:#0b0f14;--panel:#151b24;--panel2:#1c2430;--line:#2a3444;--text:#e8eaed;--muted:#9aa3b2;--accent:#e0a458;--red:#e85d5d;--blue:#4a9eff;--ok:#3dd68c;--warn:#f0b429}
*{box-sizing:border-box;margin:0;padding:0}
html,body{background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,"PingFang SC","Microsoft YaHei",sans-serif;font-size:14px;line-height:1.45;-webkit-tap-highlight-color:transparent}
body{padding-bottom:80px}
.header{position:sticky;top:0;z-index:100;background:rgba(11,15,20,.95);backdrop-filter:blur(8px);border-bottom:1px solid var(--line);padding:10px 12px;display:flex;align-items:center;justify-content:space-between}
.header h1{font-size:16px;font-weight:600;color:var(--accent)}
.header .sub{font-size:11px;color:var(--muted)}
.sec{margin:10px 10px 0;background:var(--panel);border:1px solid var(--line);border-radius:10px;overflow:hidden}
.sec-h{padding:10px 12px;background:var(--panel2);border-bottom:1px solid var(--line);font-weight:600;font-size:13px;display:flex;align-items:center;justify-content:space-between}
.sec-b{padding:10px 12px}
.row{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:8px;align-items:center}
label{font-size:12px;color:var(--muted)}
input[type=number],input[type=text],select{background:var(--bg);border:1px solid var(--line);color:var(--text);border-radius:6px;padding:6px 8px;font-size:13px;min-width:0}
input[type=number]{width:58px}
.btn{background:var(--panel2);border:1px solid var(--line);color:var(--text);border-radius:8px;padding:8px 12px;font-size:13px;cursor:pointer;touch-action:manipulation}
.btn:active{opacity:.85}
.btn-primary{background:linear-gradient(135deg,#c48a3a,#e0a458);border:none;color:#1a1208;font-weight:600}
.btn-danger{background:rgba(232,93,93,.15);border-color:var(--red);color:var(--red)}
.btn-ok{background:rgba(61,214,140,.12);border-color:var(--ok);color:var(--ok)}
.pad{display:grid;grid-template-columns:repeat(11,1fr);gap:4px}
.pad.blue{grid-template-columns:repeat(8,1fr)}
.ball{aspect-ratio:1;border-radius:50%;border:1px solid var(--line);background:var(--bg);color:var(--text);font-size:11px;display:flex;align-items:center;justify-content:center;cursor:pointer;user-select:none;touch-action:manipulation}
.ball.on{background:var(--red);border-color:var(--red);color:#fff;font-weight:600}
.ball.blue.on{background:var(--blue);border-color:var(--blue)}
.ball.kill{background:rgba(232,93,93,.25);border-color:var(--red);color:var(--red);text-decoration:line-through}
.ball.dan{background:rgba(61,214,140,.2);border-color:var(--ok);color:var(--ok);font-weight:600}
.ball.cand{background:rgba(224,164,88,.2);border-color:var(--accent);color:var(--accent)}
.tag{display:inline-block;padding:2px 6px;border-radius:4px;font-size:11px;background:var(--panel2);border:1px solid var(--line);margin:2px}
.log{font-family:ui-monospace,monospace;font-size:11px;max-height:180px;overflow:auto;background:var(--bg);padding:8px;border-radius:6px;border:1px solid var(--line);white-space:pre-wrap}
.footer{position:fixed;bottom:0;left:0;right:0;background:rgba(11,15,20,.96);border-top:1px solid var(--line);padding:8px 10px;display:flex;gap:8px;z-index:100}
.footer .btn{flex:1}
.modal{position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:200;display:none;align-items:flex-end;justify-content:center}
.modal.show{display:flex}
.modal-box{background:var(--panel);border-radius:14px 14px 0 0;width:100%;max-width:480px;max-height:80vh;overflow:auto;padding:14px}
.modal-h{font-weight:600;margin-bottom:10px;display:flex;justify-content:space-between}
.range{display:flex;gap:6px;align-items:center}
.hint{font-size:11px;color:var(--muted);margin-top:4px}
.stage{font-size:12px;padding:6px 0;border-bottom:1px dashed var(--line)}
</style>
</head>
<body>
<div class="header">
  <div>
    <h1>双色球缩水 V3.4</h1>
    <div class="sub">只缩减注数 · 不预测 · 本地运行</div>
  </div>
  <button class="btn" id="btnReset" type="button">重置</button>
</div>
<!-- PART0_END -->
