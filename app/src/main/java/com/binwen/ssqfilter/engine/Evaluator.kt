package com.binwen.ssqfilter.engine

import com.binwen.ssqfilter.model.Ticket

class Evaluator {

    fun evaluate(ticket: Ticket): Boolean {
        // 这里先返回 true，后续你会逐步加入所有结构条件
        return true
    }
}
